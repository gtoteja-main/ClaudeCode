{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    },
    {
      "src": "api/todos.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/todos(.*)", "dest": "/api/todos.js" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
